<?xml version="1.0" encoding="UTF-8"?><!DOCTYPE project [<!ELEMENT project (modelVersion|parent|groupId|artifactId|name|packaging|description|properties|dependencies|build)*><!ATTLIST projectxmlns CDATA #REQUIREDxmlns:xsi CDATA #REQUIREDxsi:schemaLocation CDATA #REQUIRED><!ELEMENT modelVersion (#PCDATA)><!ELEMENT parent (groupId|artifactId|version|relativePath)*><!ELEMENT groupId (#PCDATA)><!ELEMENT artifactId (#PCDATA)><!ELEMENT version (#PCDATA)><!ELEMENT relativePath (#PCDATA)><!ELEMENT name (#PCDATA)><!ELEMENT packaging (#PCDATA)><!ELEMENT description (#PCDATA)><!ELEMENT properties (java.version|docker.image.prefix|build.final.name|log4j2.vsersion|swagger2|swagger.ui|actuator.version|commons-lang3.version|commons-fileupload.version|shiro-spring.version|shiro-redis.version|shiro-thymeleaf-extras.version|ip2region.version|mybatis.version|mysql.version|druid.version)*><!ELEMENT java.version (#PCDATA)><!ELEMENT docker.image.prefix (#PCDATA)><!ELEMENT build.final.name (#PCDATA)><!ELEMENT log4j2.vsersion (#PCDATA)><!ELEMENT swagger2 (#PCDATA)><!ELEMENT swagger.ui (#PCDATA)><!ELEMENT actuator.version (#PCDATA)><!ELEMENT commons-lang3.version (#PCDATA)><!ELEMENT commons-fileupload.version (#PCDATA)><!ELEMENT shiro-spring.version (#PCDATA)><!ELEMENT shiro-redis.version (#PCDATA)><!ELEMENT shiro-thymeleaf-extras.version (#PCDATA)><!ELEMENT ip2region.version (#PCDATA)><!ELEMENT mybatis.version (#PCDATA)><!ELEMENT mysql.version (#PCDATA)><!ELEMENT druid.version (#PCDATA)><!ELEMENT dependencies (dependency)*><!ELEMENT dependency (groupId|artifactId|exclusions|version|scope)*><!ELEMENT exclusions (exclusion)*><!ELEMENT exclusion (groupId|artifactId)*><!ELEMENT scope (#PCDATA)><!ELEMENT build (plugins)*><!ELEMENT plugins (plugin)*><!ELEMENT plugin (groupId|artifactId|version|configuration)*><!ELEMENT configuration (imageName|imageTags|dockerDirectory|resources)*><!ELEMENT imageName (#PCDATA)><!ELEMENT imageTags (#PCDATA)><!ELEMENT dockerDirectory (#PCDATA)><!ELEMENT resources (resource)*><!ELEMENT resource (targetPath|directory|include)*><!ELEMENT targetPath (#PCDATA)><!ELEMENT directory (#PCDATA)><!ELEMENT include (#PCDATA)>]>
<project xmlns="http://maven.apache.org/POM/4.0.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>com.dimples.parent</groupId>
		<artifactId>dimples-parent</artifactId>
		<version>1.0.0</version>
		<relativePath />
	</parent>

	<groupId>com.dimples.core</groupId>
	<artifactId>dimples-core</artifactId>

	<name>dimples-core</name>
	<packaging>jar</packaging>
	<description>Springboot的核心配置模块</description>

	<properties>
		<java.version>1.8</java.version>
		<docker.image.prefix>dimples</docker.image.prefix>
		<build.final.name>dimples-core</build.final.name>
		<log4j2.vsersion>2.0.2.RELEASE</log4j2.vsersion>
		<swagger2>2.9.2</swagger2>
		<swagger.ui>2.9.2</swagger.ui>
		<actuator.version>2.1.0</actuator.version>
		<commons-lang3.version>3.8.1</commons-lang3.version>
		<commons-fileupload.version>1.3.3</commons-fileupload.version>
		<shiro-spring.version>1.4.0</shiro-spring.version>
		<shiro-redis.version>3.2.2</shiro-redis.version>
		<shiro-thymeleaf-extras.version>2.0.0</shiro-thymeleaf-extras.version>
		<ip2region.version>1.7.2</ip2region.version>
		<mybatis.version>2.1.0</mybatis.version>
		<mysql.version>8.0.18</mysql.version>
		<druid.version>1.1.21</druid.version>
	</properties>


	<dependencies>
		<!-- web -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>

		<!-- 去除系统默认的logback日志框架，使用自己配置的框架 -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter</artifactId>
			<exclusions>
				<exclusion>
					<groupId>org.springframework.boot</groupId>
					<artifactId>spring-boot-starter-logging</artifactId>
				</exclusion>
			</exclusions>
		</dependency>


		<!-- 配置 log4j2 -->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-log4j2</artifactId>
			<version>${log4j2.vsersion}</version>
		</dependency>

		<!--test测试-->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>

		<!--aop模块-->
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-aop</artifactId>
		</dependency>

		<!-- swagger -->
		<dependency>
			<groupId>io.springfox</groupId>
			<artifactId>springfox-swagger2</artifactId>
			<version>${swagger2}</version>
		</dependency>
		<!-- swagger-ui -->
		<dependency>
			<groupId>io.springfox</groupId>
			<artifactId>springfox-swagger-ui</artifactId>
			<version>${swagger.ui}</version>
		</dependency>

		<!-- Apache 工具类 -->
		<dependency>
			<groupId>org.apache.commons</groupId>
			<artifactId>commons-lang3</artifactId>
			<version>${commons-lang3.version}</version>
		</dependency>
		<dependency>
			<groupId>commons-fileupload</groupId>
			<artifactId>commons-fileupload</artifactId>
			<version>${commons-fileupload.version}</version>
		</dependency>

		<!-- shiro -->
		<dependency>
			<groupId>org.apache.shiro</groupId>
			<artifactId>shiro-spring</artifactId>
			<version>${shiro-spring.version}</version>
		</dependency>
		<dependency>
			<groupId>org.crazycake</groupId>
			<artifactId>shiro-redis</artifactId>
			<version>${shiro-redis.version}</version>
			<exclusions>
				<exclusion>
					<groupId>com.puppycrawl.tools</groupId>
					<artifactId>checkstyle</artifactId>
				</exclusion>
			</exclusions>
		</dependency>
		<!-- shiro + thymeleaf 实现页面级权限控制-->
		<!--<dependency>
			<groupId>com.github.theborakompanioni</groupId>
			<artifactId>thymeleaf-extras-shiro</artifactId>
			<version>${shiro-thymeleaf-extras.version}</version>
		</dependency>-->

		<!-- ip获取地址 -->
		<dependency>
			<groupId>org.lionsoul</groupId>
			<artifactId>ip2region</artifactId>
			<version>${ip2region.version}</version>
		</dependency>
		<!-- Mybatis -->
		<dependency>
			<groupId>org.mybatis.spring.boot</groupId>
			<artifactId>mybatis-spring-boot-starter</artifactId>
			<version>${mybatis.version}</version>
		</dependency>
		<!-- mysql/mysql-connector-java -->
		<dependency>
			<groupId>mysql</groupId>
			<artifactId>mysql-connector-java</artifactId>
			<version>${mysql.version}</version>
		</dependency>
		<!-- com.alibaba/druid -->
		<dependency>
			<groupId>com.alibaba</groupId>
			<artifactId>druid</artifactId>
			<version>${druid.version}</version>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
			</plugin>
			<plugin>
				<groupId>com.spotify</groupId>
				<artifactId>docker-maven-plugin</artifactId>
				<version>0.4.11</version>
				<configuration>
					<imageName>${docker.image.prefix}/${project.artifactId}</imageName>
					<imageTags>dimples</imageTags>
					<!-- 指定Dockerfile文件位置 -->
					<dockerDirectory>${project.basedir}</dockerDirectory>
					<resources>
						<resource>
							<targetPath>/</targetPath>
							<directory>${project.build.directory}</directory>
							<include>${build.final.name}-${version}.jar</include>
						</resource>
					</resources>
				</configuration>
			</plugin>
		</plugins>
	</build>

</project>
